describe ('User Onboarding App', () => {
    beforeEach(() => {
        cy.visit('http://localhost:3002')
    })

    const textInput = () => cy.get('input[name="text"]')
    const textInput = () => cy.get('input[name="text"]')
    const textInput = () => cy.get('input[name="text"]')
    const textInput = () => cy.get('input[name="text"]')
})